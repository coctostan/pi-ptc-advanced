---
phase: 25-response-and-file-handle-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/contracts/handle-types.ts
  - src/types.ts
  - src/tool-adapters.ts
  - test/contracts-public-types.test.ts
  - test/tool-adapters.test.ts
autonomous: true
---

<objective>
## Goal
Define and verify a non-breaking response/file handle contract inside `pi-ptc-next` so Phase 26 can add Python-facing helper ergonomics without inventing a new cross-repo protocol.

## Purpose
This keeps `pi-ptc-next` in its intended role as orchestration glue: it should understand the existing `responseId` and `filePath` follow-up surfaces already emitted by `pi-web-tools`, while preserving current `details.ptcValue` passthrough and fallback behavior.

## Output
- A local handle-contract module covering only response and file handles
- Adapter-level extraction/normalization helpers for existing `pi-web-tools` payload shapes
- Focused regression coverage proving the new contract is explicit and non-breaking
</objective>

<context>
## Project Context
.paul/PROJECT.md
.paul/ROADMAP.md
.paul/STATE.md

## Prior Work (only if genuinely needed)
# No prior SUMMARY is required for this plan.
# This phase is a fresh contract slice for Milestone 10.

## Source Files
src/tool-adapters.ts
src/types.ts
src/contracts/execution-types.ts
src/tools/tool-wrapper.ts
test/tool-adapters.test.ts
test/contracts-public-types.test.ts
../pi-web-tools/index.ts
../pi-web-tools/README.md
</context>

<module_dispatch>
## Pre-Plan Module Dispatch
[dispatch] pre-plan advisory: TODD(100) → tdd_candidates found | IRIS(150) → 0 review flags | DAVE(200) → ci inventory found | DOCS(200) → doc catalog found | RUBY(250) → 0 debt flags

- TODD: Existing focused `node:test` coverage already targets adapter normalization and public contract import paths, so TDD is viable. This plan remains `execute` to keep the phase centered on contract definition and non-breaking normalization, but RED/GREEN restructuring can be requested before APPLY if desired.
- IRIS: No obvious anti-pattern signatures surfaced from the current target files during planning.
- DAVE: `.github/workflows/ci.yml` already exists, so this phase can rely on the repo-owned build/test path instead of inventing new CI surfaces.
- DOCS: Maintainer docs already exist (`README.md`, `docs/programmatic-tool-calling.md`), but dedicated user-facing handle-helper documentation is not present yet; that documentation work is intentionally deferred to Phase 26.
- RUBY: Current target files are small and focused; no pre-plan debt escalation is indicated for this slice.

[dispatch] pre-plan enforcement: DEAN(50) → baseline recorded
- DEAN baseline audit: critical 0 / high 2 / moderate 1 / low 1.
- Result: no block (critical baseline is zero), but this milestone must not introduce new critical/high vulnerabilities.
</module_dispatch>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)
No specialized flows configured (`.paul/SPECIAL-FLOWS.md` not found).
</skills>

<acceptance_criteria>
## AC-1: Existing response/file follow-up surfaces have an explicit local contract
```gherkin
Given `pi-web-tools` results that already expose `responseId` and/or `filePath`
When `pi-ptc-next` inspects those normalized values
Then it can identify typed response/file handle references without changing the upstream payload shape or scraping prose output
```

## AC-2: Adapter normalization remains non-breaking
```gherkin
Given current built-in fallback behavior and current `details.ptcValue` passthrough semantics
When the handle contract work lands
Then `normalizeToolResult()` still preserves existing fallback-safe behavior and still returns upstream `ptcValue` payloads unchanged for consumers that already rely on them
```

## AC-3: The new contract is covered by focused regression proof
```gherkin
Given mocked `pi-web-tools` payloads for response-only, file-offloaded, and mixed result cases
When the focused regression tests run
Then they prove the handle extraction contract works for the supported shapes and rejects unsupported/non-handle cases cleanly
```
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Task 1: Introduce the response/file handle contract surface</name>
  <files>src/contracts/handle-types.ts, src/types.ts, test/contracts-public-types.test.ts</files>
  <action>
    Add a dedicated contract module that defines only the handle shapes this milestone supports:
    - response handles for existing `responseId`-based follow-up
    - file handles for existing `filePath`-based follow-up
    Re-export the relevant public types through `src/types.ts` so the contract is available from the repo's public type surface.
    Extend focused import coverage so the new contract module is built and directly importable in dist tests.
    Avoid: graph/trace/record handle abstractions, package export churn, or any protocol changes in adjacent repos.
  </action>
  <verify>npm run build && node --test test/contracts-public-types.test.ts</verify>
  <done>AC-1 and AC-3 satisfied for the contract surface: supported handle shapes are explicit, bounded to response/file flows, and importable.</done>
</task>

<task type="auto">
  <name>Task 2: Add non-breaking adapter-side handle extraction for existing web-tools payloads</name>
  <files>src/tool-adapters.ts</files>
  <action>
    Add helper logic in the adapter layer that inspects current normalized `ptcValue` payloads and extracts supported response/file handles from existing `pi-web-tools` shapes.
    Keep `normalizeToolResult()` behavior stable:
    - preserve passthrough of upstream `details.ptcValue`
    - preserve current text fallbacks when `ptcValue` is absent
    - do not parse human-readable prose for handles
    Make the extraction logic intentionally narrow to the known Milestone 10 scope (`responseId`, `filePath`) and treat graph-handle support as deferred.
    Avoid: wrapping existing tool values in a breaking envelope, changing RPC behavior, or adding Python-facing helper APIs in this phase.
  </action>
  <verify>npm run build && node --test test/tool-adapters.test.ts</verify>
  <done>AC-1 and AC-2 satisfied: supported handles are detectable from current structured payloads without breaking existing normalized result consumers.</done>
</task>

<task type="auto">
  <name>Task 3: Prove supported and unsupported handle cases with focused regressions</name>
  <files>test/tool-adapters.test.ts, test/contracts-public-types.test.ts</files>
  <action>
    Add mocked `pi-web-tools` result fixtures that cover:
    - response-handle cases carrying `responseId`
    - file-handle cases carrying `filePath`
    - mixed/multi-result cases where both may appear
    - negative cases that should not be classified as supported handles
    Assert both sides of the contract:
    - handle extraction returns the expected typed references
    - existing normalized tool values remain unchanged for passthrough/fallback behavior
    Keep the test surface focused on this phase's contract slice; do not expand into Python helper ergonomics or end-to-end docs/examples yet.
  </action>
  <verify>npm run build && node --test test/tool-adapters.test.ts test/contracts-public-types.test.ts</verify>
  <done>AC-1 through AC-3 satisfied with focused proof for supported handle extraction and non-breaking normalization behavior.</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- `../pi-web-tools/**` and any other adjacent repository files
- `src/python-runtime/runtime.py` and Python-facing helper ergonomics for this phase
- `src/tools/tool-wrapper.ts` generated helper surface unless required by an unavoidable compile/type issue
- README/docs user-facing milestone documentation in this phase
- Current hashline-native `read` / `grep` / `edit` passthrough semantics outside the narrow response/file handle contract work

## SCOPE LIMITS
- No new cross-repo protocol or payload-shape changes
- No graph/trace/record handle standardization in this plan
- No prose scraping fallback for handle discovery
- No end-to-end recipe/demo expansion beyond focused regression proof
</boundaries>

<verification>
Before declaring plan complete:
- [ ] `npm run build`
- [ ] `node --test test/tool-adapters.test.ts test/contracts-public-types.test.ts`
- [ ] Existing `normalizeToolResult()` passthrough/fallback assertions still pass alongside the new handle tests
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- A dedicated response/file handle contract exists in repo code
- Adapter logic can extract supported handles from current structured payloads without changing the returned normalized value
- Focused regressions cover positive and negative handle cases
- No adjacent repo changes or Python-helper/doc scope creep were introduced
</success_criteria>

<output>
After completion, create `.paul/phases/25-response-and-file-handle-contract/25-01-SUMMARY.md`
</output>
