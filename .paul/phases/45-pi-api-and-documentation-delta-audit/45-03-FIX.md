---
phase: 45-pi-api-and-documentation-delta-audit
plan: 03
type: fix
wave: 1
depends_on: [45-02]
files_modified:
  - .github/workflows/ci.yml
  - .pi/evals/ptc/baselines/local__seeded__recipes.json
  - .pi/evals/ptc/recipes/codegraph-web-evidence-merge.py
  - .pi/evals/ptc/recipes/graph-compact-ranking.py
  - .pi/evals/ptc/recipes/hashline-anomaly-summary.py
  - .pi/evals/ptc/recipes/web-answer-comparison.py
autonomous: true
---

<objective>
## Fix
Close the remaining 26 CI failures revealed by Fix 45-02. Three
pre-existing CI-environment gaps are in scope, all bounded:

1. **Force-add 5 missing eval fixtures** that the test suite reads
   (`.pi/evals/ptc/baselines/local__seeded__recipes.json` and four
   recipe `.py` files). `.gitignore:8 .pi/` ignores `.pi/` by default;
   the existing eval `cases/*.json` files were already `git add -f`'d
   the same way, so adopting the same pattern is consistent.

2. **Install `pi-hashline-readmap` on CI** so the hashline contract
   tests can find it via `require.resolve("pi-hashline-readmap")`.
   Add `npm install -g pi-hashline-readmap@^0.8.6` as a workflow step
   so the existing resolution candidates inside
   `test/hashline-read-contract.test.ts:50-60` find it without
   needing `PI_HASHLINE_READMAP_ROOT`.

3. **Install `ast-grep` (`sg`) on CI** so
   `test/hashline-real-interop.test.ts` and
   `test/hashline-sg-contract.test.ts` can find the binary on `PATH`.
   Add `npm install -g @ast-grep/cli` as a workflow step.

After this fix, CI should run the full 207-test suite and report
SUCCESS, clearing the Phase 45 merge gate for PR #1 and unblocking
Phase 46 (`require_pr_before_next_phase: true`).
</objective>

<tasks>

<task type="auto">
  <name>Fix: Force-add missing eval fixture files</name>
  <files>.pi/evals/ptc/baselines/local__seeded__recipes.json, .pi/evals/ptc/recipes/codegraph-web-evidence-merge.py, .pi/evals/ptc/recipes/graph-compact-ranking.py, .pi/evals/ptc/recipes/hashline-anomaly-summary.py, .pi/evals/ptc/recipes/web-answer-comparison.py</files>
  <action>
Run `git add -f` on each of the five fixture paths. They already exist
locally; this just makes them visible to the CI runner. Match the
existing pattern used for `.pi/evals/ptc/cases/*.json`.
  </action>
  <verify>`git ls-files .pi/evals/ptc/baselines/ .pi/evals/ptc/recipes/` lists all 5 files; `npm run build` still passes locally.</verify>
  <done>The 5 eval fixture files are tracked by git.</done>
</task>

<task type="auto">
  <name>Fix: Add ast-grep and pi-hashline-readmap install steps to CI workflow</name>
  <files>.github/workflows/ci.yml</files>
  <action>
After the existing `Install dependencies` step and before
`Run repo verification suite`, insert two steps:

```yaml
      - name: Install ast-grep CLI
        run: npm install -g @ast-grep/cli

      - name: Install pi-hashline-readmap
        run: npm install -g pi-hashline-readmap@^0.8.6
```

Both are global installs so the existing test resolution paths
(`execFileSync("sg", ...)` and `require.resolve("pi-hashline-readmap")`)
work without further changes.
  </action>
  <verify>`grep -nE 'ast-grep|pi-hashline-readmap' .github/workflows/ci.yml` shows both install steps; `cat .github/workflows/ci.yml` shows the steps in the right order (after `npm ci`, before `npm run verify:ci`).</verify>
  <done>Workflow installs both tools globally before running the verification suite.</done>
</task>

<task type="auto">
  <name>Fix: Commit, push, and confirm CI passes</name>
  <files>.github/workflows/ci.yml, .pi/evals/ptc/baselines/local__seeded__recipes.json, .pi/evals/ptc/recipes/codegraph-web-evidence-merge.py, .pi/evals/ptc/recipes/graph-compact-ranking.py, .pi/evals/ptc/recipes/hashline-anomaly-summary.py, .pi/evals/ptc/recipes/web-answer-comparison.py</files>
  <action>
Stage the workflow change plus the 5 force-added fixtures, commit with
a `fix(ci):` conventional message naming the three gap classes,
push to `origin/feat/hashline-native-interop`, then poll PR #1 CI
status until both `Verify release baseline` runs complete.
  </action>
  <verify>`gh pr view 1 --repo coctostan/pi-ptc-next --json statusCheckRollup` reports `Verify release baseline` conclusion `SUCCESS` for the latest commit.</verify>
  <done>CI on PR #1 reports SUCCESS for `Verify release baseline`; PR #1 is mergeable; merge gate is clear.</done>
</task>

</tasks>

<verification>
- [ ] All 5 fixture files are tracked by `git ls-files`
- [ ] `.github/workflows/ci.yml` installs `@ast-grep/cli` and `pi-hashline-readmap@^0.8.6`
- [ ] `npm run build` passes locally
- [ ] CI `Verify release baseline` on PR #1 reports SUCCESS
- [ ] No `src/**`, `test/**`, or `package.json` changes
</verification>
