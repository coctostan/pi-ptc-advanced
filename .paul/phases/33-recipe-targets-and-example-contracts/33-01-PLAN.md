---
phase: 33-recipe-targets-and-example-contracts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/eval-cases.ts
  - src/benchmark-runner.ts
  - .pi/evals/ptc/cases/recipe-graph-compact-ranking.json
  - .pi/evals/ptc/cases/recipe-web-answer-comparison.json
  - .pi/evals/ptc/cases/recipe-hashline-anomaly-summary.json
  - .pi/evals/ptc/cases/recipe-codegraph-web-evidence-merge.json
  - test/eval-cases.test.ts
  - test/benchmark-runner.test.ts
autonomous: true
---

<objective>
## Goal
Add a machine-readable recipe-target contract for M4 ecosystem examples and seed deterministic recipe cases that cover the four intended workflow shapes without reopening runtime semantics or user-facing docs yet.

## Purpose
Milestone 13 needs a stable, testable definition of which ecosystem recipes this repo should demonstrate before adding broader recipe artifacts and final docs. That keeps `pi-ptc-next` generic while giving later phases concrete adjacent-repo targets and bounded output expectations to build against.

## Output
- Additive eval/benchmark typing for recipe-target metadata and compact output-contract expectations.
- Four deterministic seeded recipe cases covering graph ranking, URL comparison, hashline anomaly summarization, and mixed codegraph+web evidence merge.
- Focused tests that prove the new recipe metadata is parsed and consumed deterministically.
</objective>

<context>
## Project Context
.paul/PROJECT.md
.paul/ROADMAP.md
.paul/STATE.md

## Prior Work
.paul/phases/32-proof-and-ecosystem-docs/32-01-SUMMARY.md

## Source Files
src/eval-cases.ts
src/benchmark-runner.ts
.pi/evals/ptc/cases/ptc-positive-multi-file-aggregation.json
.pi/evals/ptc/cases/ptc-positive-repo-count-ranking.json
test/eval-cases.test.ts
test/benchmark-runner.test.ts
</context>

<module_dispatch>
## Pre-Plan Module Dispatch
[dispatch] pre-plan advisory: ARCH(75) → arch_context | TODD(100) → tdd_candidates,tdd_type | IRIS(150) → review_flags | DAVE(200) → deploy_context | DOCS(200) → doc_warnings | RUBY(250) → debt_flags
[dispatch] pre-plan enforcement: DEAN(50) → dep_warnings

- **DEAN:** `npm audit --json` remains non-blocking at `0 critical / 2 high / 2 moderate / 1 low`; no baseline file is present, but the workflow only blocks on critical issues when no baseline exists.
- **ARCH:** The repo still reads as a layered TypeScript package (`src/contracts`, `src/execution`, `src/tools`) with benchmark/eval infrastructure in `src/benchmark-runner.ts` and `.pi/evals/ptc/cases`. Planned files stay in their expected layer. Structural warning: `src/benchmark-runner.ts` is already `466` lines, so changes must stay additive and compact.
- **TODD:** Node's built-in test runner is active via `package.json` and the repo already has focused suites for eval/benchmark behavior. This plan can stay `execute`, but every contract change should land with targeted tests in `test/eval-cases.test.ts` and `test/benchmark-runner.test.ts`.
- **IRIS:** No `TODO` / `FIXME` / `HACK` / `XXX` / `any` / `eval(` hits were found in the planned existing files. Debt warning: `src/benchmark-runner.ts` (`466` lines) and `test/benchmark-runner.test.ts` (`315` lines) are current hotspots; avoid broadening scope beyond the recipe-contract slice.
- **DAVE:** `.github/workflows/ci.yml` exists and delegates to `npm run verify:ci`; planned changes should preserve `npm run build` and targeted test viability without introducing new CI surface area.
- **DOCS:** Nearby docs exist (`README.md`, `docs/PTC-RESEARCH.md`, `docs/programmatic-tool-calling.md`), but this phase intentionally defers user-facing doc updates to Phase 35. Record any doc drift risk without expanding scope now.
- **RUBY:** Preliminary debt scan matches IRIS: keep modifications focused because the benchmark runner and its test already exceed the soft 300-line warning threshold.
- **Skipped modules:** ARIA, DANA, GABE, LUKE, OMAR, PETE, REED, SETH, and VERA have no UI/data/API/observability/security-specific triggers in the planned file set.
</module_dispatch>

<acceptance_criteria>
## AC-1: Eval-case schema can describe recipe targets additively
```gherkin
Given the existing deterministic eval-case and benchmark infrastructure
When M4 recipe cases are parsed and loaded
Then the schema accepts additive recipe-target metadata and compact output-contract expectations without breaking existing case validation or routing semantics
```

## AC-2: Seeded recipe cases cover the intended M4 workflow shapes
```gherkin
Given the seeded eval case directory under `.pi/evals/ptc/cases`
When the new recipe cases are inspected by tests and benchmark loading code
Then the repo contains deterministic cases for graph ranking, multi-URL comparison, hashline anomaly summarization, and mixed codegraph-plus-web evidence merging with bounded-output expectations
```

## AC-3: Benchmark-facing consumption remains deterministic
```gherkin
Given the additive recipe metadata and new seeded cases
When focused benchmark tests exercise loading and deterministic run generation
Then recipe-tagged cases are consumed predictably, existing summaries/comparisons remain stable, and no broader runtime or docs surfaces need to change in this phase
```
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Task 1: Extend eval and benchmark contracts for recipe-target metadata</name>
  <files>src/eval-cases.ts, src/benchmark-runner.ts</files>
  <action>
    Add an additive typed recipe-target shape to the eval-case contract so seeded cases can declare adjacent-repo target, workflow category, and compact output-contract expectations for M4 examples.
    Keep the existing required routing/acceptance fields intact and preserve current benchmark comparison semantics.
    If benchmark loading or emitted records need recipe metadata for deterministic downstream use, expose it additively without redesigning the benchmark runner or introducing a broader recipe DSL.
    Avoid touching `src/python-runtime/runtime.py`, helper semantics, or user-facing docs in this phase.
  </action>
  <verify>npm run build && node --test test/eval-cases.test.ts test/benchmark-runner.test.ts</verify>
  <done>AC-1 and AC-3 satisfied: recipe metadata is modeled additively and existing benchmark behavior remains stable.</done>
</task>

<task type="auto">
  <name>Task 2: Seed deterministic M4 recipe cases for adjacent ecosystem workflows</name>
  <files>.pi/evals/ptc/cases/recipe-graph-compact-ranking.json, .pi/evals/ptc/cases/recipe-web-answer-comparison.json, .pi/evals/ptc/cases/recipe-hashline-anomaly-summary.json, .pi/evals/ptc/cases/recipe-codegraph-web-evidence-merge.json, test/eval-cases.test.ts</files>
  <action>
    Add four stable JSON eval cases aligned to the M4 build-map workflows:
    - analyze many graph results and return a compact ranking
    - compare answers from multiple URLs
    - inspect many files and summarize only anomalies
    - merge codegraph and web-tools evidence into one bounded result
    Keep prompts deterministic, repo-agnostic where possible, and expressed as compact behavioral/output-contract expectations instead of live integration assumptions.
    Update seeded-case inventory assertions so the new recipe cases become part of the explicit supported corpus.
    Avoid introducing flaky network/runtime dependencies or coupling the cases to a single provider.
  </action>
  <verify>node --test test/eval-cases.test.ts</verify>
  <done>AC-2 satisfied: the seeded eval corpus now covers all four M4 recipe shapes with deterministic contracts.</done>
</task>

<task type="auto">
  <name>Task 3: Add focused benchmark proof for recipe-tagged case consumption</name>
  <files>test/benchmark-runner.test.ts</files>
  <action>
    Extend the focused benchmark-runner coverage so recipe-tagged cases are loaded, surfaced, and preserved deterministically through the existing benchmark flow.
    Prefer narrow assertions around schema/load/run behavior over broad end-to-end harness expansion.
    Keep tests in the focused benchmark/eval suites instead of widening omnibus files like `test/index.test.ts` or `test/code-executor.test.ts`.
  </action>
  <verify>node --test test/benchmark-runner.test.ts</verify>
  <done>AC-3 satisfied: deterministic benchmark coverage proves recipe-target metadata works without broader runtime or docs changes.</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- README.md, docs/**, and src/index.ts (Phase 35 owns user-facing recipe docs and generated guidance alignment)
- src/python-runtime/runtime.py and src/code-executor.ts (no new runtime/helper semantics in this phase)
- Existing orchestration helper behavior from Milestones 10-12 unless an additive eval/benchmark typing change is strictly required

## SCOPE LIMITS
- No live adjacent-repo integration harness or network-dependent proof
- No benchmark-runner redesign beyond additive recipe metadata support
- No new orchestration helper APIs, handle semantics, or broader validation framework
- No doc sweep; capture doc drift for later phases instead of expanding scope here
</boundaries>

<verification>
Before declaring plan complete:
- [ ] npm run build
- [ ] node --test test/eval-cases.test.ts
- [ ] node --test test/benchmark-runner.test.ts
- [ ] npm audit --json confirms the existing non-critical baseline does not worsen
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- All tasks completed
- All focused verification checks pass
- Existing seeded eval cases continue to validate
- Four deterministic M4 recipe cases exist and are locked by tests
- Recipe-target metadata remains additive and does not force runtime/helper/doc changes in this phase
</success_criteria>

<output>
After completion, create `.paul/phases/33-recipe-targets-and-example-contracts/33-01-SUMMARY.md`
</output>
