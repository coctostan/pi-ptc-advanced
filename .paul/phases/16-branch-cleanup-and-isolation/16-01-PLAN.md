---
phase: 16-branch-cleanup-and-isolation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .paul/phases/16-branch-cleanup-and-isolation/16-01-BRANCH-ISOLATION.md
  - README.md
  - docs/hashline-integration/START-HERE.md
  - docs/hashline-integration/DEMO.md
  - docs/hashline-integration/HARNESS-EVALUATION.md
  - docs/hashline-integration/ROADMAP.md
  - docs/hashline-integration/MILESTONE-01.md
  - pals.json
  - scripts/start-pi-ptc-full-tools.sh
autonomous: true
---

<objective>
## Goal
Turn the Phase 15 scope lock into an executable branch-cleanup pass that isolates the upstream-ready PTC work from local-only planning artifacts, personal tooling, and optional companion docs.

## Purpose
This phase makes the branch reviewer-appropriate without reopening the scope decision. It protects the core value of upstreaming the actual `code_execution` interop/product work while preventing local baggage from leaking into the eventual PR slices.

## Output
- `.paul/phases/16-branch-cleanup-and-isolation/16-01-BRANCH-ISOLATION.md` containing:
  - exact PR1 / PR2 / local-only / follow-up file classification for the current branch delta
  - explicit disposition for optional docs and heavier local harness artifacts
  - reproducible verification commands for later branch-splitting / PR-prep work
- a cleaned reviewer-facing doc surface in `README.md`
- local-only loose files removed or relocated from the active submission path
</objective>

<context>
## Project Context
.paul/PROJECT.md
.paul/ROADMAP.md
.paul/STATE.md

## Prior Work (only if genuinely needed)
.paul/phases/15-upstream-scope-audit/15-01-SUMMARY.md
.paul/phases/15-upstream-scope-audit/15-01-UPSTREAM-SCOPE.md

## Source Files
README.md
docs/hashline-integration/START-HERE.md
docs/hashline-integration/DEMO.md
docs/hashline-integration/HARNESS-EVALUATION.md
docs/hashline-integration/ROADMAP.md
docs/hashline-integration/MILESTONE-01.md
scripts/start-pi-ptc-full-tools.sh
pals.json
.gitignore
test/e2e-agent-harness.sh
test/hashline-real-interop.mjs
test/hashline-real-interop.test.ts
test/hashline-interop-smoke.test.ts
test/hashline-default-exposure.test.ts
test/utils.test.ts
src/index.ts
src/tool-registry.ts
src/tools/python-tool-contract.ts
src/tools/tool-wrapper.ts
src/python-runtime/runtime.py
</context>

<acceptance_criteria>
## AC-1: Branch contents are explicitly classified for upstream isolation
```gherkin
Given the current branch contains both upstream-worthy product changes and local-only artifacts
When Phase 16 cleanup/isolation completes
Then there is a durable artifact that classifies the current delta into PR 1, PR 2, local-only, or follow-up groups with an explicit rationale for each non-obvious item
```

## AC-2: The maintainer-facing docs stand on their own for first-pass upstream review
```gherkin
Given README.md is the canonical reviewer-visible documentation surface
When cleanup completes
Then the upstream submission no longer depends on personal launcher scripts or local-only companion docs to explain the shipped behavior and verification story
```

## AC-3: The repo has a reproducible path to PR-ready branch isolation
```gherkin
Given the cleanup decisions are locked for this milestone
When verification runs
Then the active submission path is free of newly introduced local-only loose files, no repo-specific compatibility shims are present in the intended upstream surface, and Phase 18 has exact commands/file groups to prepare the final PR slices
```
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Task 1: Create the exact branch-isolation manifest for the current delta</name>
  <files>.paul/phases/16-branch-cleanup-and-isolation/16-01-BRANCH-ISOLATION.md</files>
  <action>
    Create the phase artifact that converts the Phase 15 scope audit into a file-level execution map.
    - Compare the current branch against `origin/main` and also inspect the current untracked working-tree additions.
    - Group the relevant files into: PR 1 runtime interop core, PR 2 metadata/helper/docs, local-only exclusions, and follow-up / optional items.
    - Explicitly classify non-obvious artifacts that were not fully resolved by the prior high-level audit, including `test/e2e-agent-harness.sh`, `test/hashline-real-interop.mjs`, `test/hashline-real-interop.test.ts`, the untracked `docs/hashline-integration/*` files, `pals.json`, `scripts/start-pi-ptc-full-tools.sh`, and the already-committed `.paul/**` history.
    - For each excluded or deferred item, state whether the outcome is: remove now, keep local-only, or isolate later via branch-splitting rather than source edits.
    - Avoid vague buckets like "extra tests" or "misc docs"; the artifact must be precise enough that Phase 18 can execute branch prep without rediscovery.
  </action>
  <verify>test -f .paul/phases/16-branch-cleanup-and-isolation/16-01-BRANCH-ISOLATION.md && grep -q "## PR 1 Candidate Surface" .paul/phases/16-branch-cleanup-and-isolation/16-01-BRANCH-ISOLATION.md && grep -q "## Local-only Exclusions" .paul/phases/16-branch-cleanup-and-isolation/16-01-BRANCH-ISOLATION.md</verify>
  <done>AC-1 satisfied: the branch delta is explicitly classified for upstream isolation.</done>
</task>

<task type="auto">
  <name>Task 2: Clean the reviewer-facing doc surface and remove loose local-only baggage</name>
  <files>README.md, docs/hashline-integration/START-HERE.md, docs/hashline-integration/DEMO.md, docs/hashline-integration/HARNESS-EVALUATION.md, docs/hashline-integration/ROADMAP.md, docs/hashline-integration/MILESTONE-01.md, pals.json, scripts/start-pi-ptc-full-tools.sh</files>
  <action>
    Align the working tree with the scope audit so the upstream-facing surface is reviewer-appropriate.
    - Decide the first-pass treatment for `START-HERE.md`, `DEMO.md`, and `HARNESS-EVALUATION.md`: distill any truly needed maintainer-facing guidance into `README.md`, then keep or remove the local-only docs accordingly.
    - Remove or relocate the untracked personal/local-only files (`pals.json`, `scripts/start-pi-ptc-full-tools.sh`, and any excluded hashline-integration side docs) so they do not remain as accidental PR baggage in `git status`.
    - If README currently points reviewers at local-only companion docs, rewrite it so the first upstream pass is understandable from README plus the in-scope tests.
    - Do not change runtime behavior or retune the upstream-worthy test suite in this task; this is surface cleanup, not implementation hardening.
  </action>
  <verify>test -z "$(git status --short --untracked-files=all | grep -E '^(\?\?|[ MARCUD]{2}) (docs/hashline-integration/|pals\.json|scripts/start-pi-ptc-full-tools\.sh)')" && ! grep -q 'docs/hashline-integration/' README.md</verify>
  <done>AC-2 satisfied: the first-pass reviewer surface no longer depends on local-only companion material.</done>
</task>

<task type="auto">
  <name>Task 3: Record the exact verification and branch-prep handoff for later PR packaging</name>
  <files>.paul/phases/16-branch-cleanup-and-isolation/16-01-BRANCH-ISOLATION.md</files>
  <action>
    Finish the isolation artifact with the deterministic checks that later phases should trust.
    - Add the exact verification commands for confirming the intended upstream surface, including `origin/main...HEAD` file checks and the final PR 1 / PR 2 groupings.
    - Search the intended upstream source and docs surface for repo-specific compatibility shims or adjacent-repo leakage (for example `pi-codegraph`) and record the result.
    - Call out any remaining isolation debt that should be handled by branch-splitting/cherry-picking in Phase 18 rather than by editing source files in this phase.
    - Keep the artifact operational: Phase 18 should be able to read it and immediately know what to include, what to exclude, and what to verify.
  </action>
  <verify>grep -q "## Verification Commands" .paul/phases/16-branch-cleanup-and-isolation/16-01-BRANCH-ISOLATION.md && grep -q "## Phase 18 Branch-Prep Handoff" .paul/phases/16-branch-cleanup-and-isolation/16-01-BRANCH-ISOLATION.md && ! grep -R "pi-codegraph" src README.md test/hashline-interop-smoke.test.ts test/hashline-default-exposure.test.ts test/utils.test.ts</verify>
  <done>AC-3 satisfied: Phase 18 has a reproducible isolation/verification handoff and the intended upstream surface is free of repo-specific shim leakage.</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- `src/**` runtime logic and callable/policy behavior in this phase
- the focused upstream-worthy tests unless a file must only be classified/excluded rather than behaviorally changed
- package versioning, `CHANGELOG.md`, release packaging, or CI setup
- adjacent repos such as `pi-codegraph` and `pi-hashline-readmap`
- existing branch history; do not rewrite commits or create final submission branches in this phase

## SCOPE LIMITS
- Do not redo the scope audit; use Phase 15 as the authority for what is in vs out
- Do not turn this phase into acceptance hardening; Phase 17 owns test/doc tightening
- Do not open PRs or perform final branch-splitting/cherry-pick work yet; only make that later step deterministic
- Do not introduce new PTC features, helper expansion, or ecosystem recipes
</boundaries>

<verification>
Before declaring plan complete:
- [ ] `.paul/phases/16-branch-cleanup-and-isolation/16-01-BRANCH-ISOLATION.md` exists
- [ ] The artifact contains PR 1, PR 2, local-only exclusion, verification-command, and Phase 18 handoff sections
- [ ] README no longer points reviewers at local-only `docs/hashline-integration/*` material
- [ ] `git status --short --untracked-files=all` no longer shows local-only loose files under `docs/hashline-integration/`, `pals.json`, or `scripts/start-pi-ptc-full-tools.sh`
- [ ] Searches of the intended upstream surface show no repo-specific compatibility shim leakage
- [ ] All acceptance criteria are satisfied
</verification>

<success_criteria>
- All tasks completed
- The branch-isolation decisions are explicit enough to drive later branch prep without rediscovery
- The working tree no longer carries accidental local-only baggage in the active submission path
- Phase 17 can focus on hardening only the retained upstream surface
</success_criteria>

<output>
After completion, create `.paul/phases/16-branch-cleanup-and-isolation/16-01-SUMMARY.md`
</output>
