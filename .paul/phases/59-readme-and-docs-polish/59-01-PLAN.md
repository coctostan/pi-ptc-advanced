---
phase: 59-readme-and-docs-polish
plan: 01
type: tdd
wave: 1
depends_on:
  - .paul/phases/58-public-identity-rename/58-01-SUMMARY.md
files_modified:
  - test/release-readiness.test.ts
  - README.md
  - docs/personal-fork-maintenance.md
  - docs/releases/1.0.0.md
autonomous: true
---

<objective>
## Goal
Polish the public-facing README and release-maintainer docs for `pi-ptc-advanced@1.0.0` so a new user can understand what the extension does, install it, run a quickstart, find the helper/API surface, verify the package, and understand limitations without being led through historical fork framing.

## Purpose
Phase 58 established the mechanical public identity baseline. Phase 59 turns that baseline into a coherent user-facing documentation experience. The README should function as the public package front door; maintainer docs should support release verification and manual boundaries without using old personal-fork framing as the main narrative.

## Output
- README substantially rewritten into a user-first public package guide with quickstart, usage, helper overview, configuration, verification, limitations, development, and credits/lineage sections.
- Public release maintenance docs retitled/reframed away from personal-fork wording while preserving manual publish/repo-rename boundaries.
- `docs/releases/1.0.0.md` enriched enough to support the polished README without claiming publish, tags, GitHub releases, or repo rename occurred.
- Release-readiness drift tests extended to guard the README/docs structure and stale framing.
</objective>

<context>
## Project Context
.paul/PROJECT.md
.paul/ROADMAP.md
.paul/STATE.md

## Current Milestone Context
Milestone 20 targets `pi-ptc-advanced@1.0.0` as a clean public NPM release. Phase 58 completed the public identity rename baseline:
- Package metadata and release verifier now target `pi-ptc-advanced@1.0.0`.
- README already starts with `# pi-ptc-advanced`, but its body still carries older organization and deep technical sections before a concise newcomer path.
- `docs/personal-fork-maintenance.md` was retitled to Public Release Maintenance, but the filename remains unchanged for compatibility; this phase may polish tone and cross-links, not necessarily rename the file.
- Actual `npm publish`, git tags, GitHub releases, and GitHub repository rename remain user-owned/manual.
- User clarification after plan review: make the README rewrite larger rather than a small reorder; leave npm install commands out for now because actual npm publish has not been user-confirmed.

## Prior Work
- `.paul/phases/58-public-identity-rename/58-01-SUMMARY.md` — `pi-ptc-advanced@1.0.0` identity baseline complete; Phase 59 explicitly owns broader README/docs polish.
- README reviewer skill loaded for this planning session. Relevant guidance: README must answer what/why/how-to-run/how-to-use/how-to-get-help quickly; every claim should be verifiable from repo files.

## Source Files
README.md
docs/personal-fork-maintenance.md
docs/releases/1.0.0.md
test/release-readiness.test.ts
package.json
CHANGELOG.md
.github/workflows/ci.yml
LICENSE
</context>

<skills>
- `/skill:readme-reviewer` — invoked during PLAN creation. APPLY may proceed after confirming this skill remains loaded or by recording an explicit override.
</skills>

<module_dispatch>
## Pre-Plan Module Dispatch

[dispatch] pre-plan advisory: installed `modules.yaml` loaded (kernel 2.0.0); README/docs polish scope evaluated across 4 planned files plus package/CI/license context.

- **TODD (advisory):** test infrastructure detected (`package.json` uses `node --test`; `test/release-readiness.test.ts` already guards active README/runbook/release docs). Plan type set to `tdd`; Task 1 must update docs/readiness assertions first and record RED evidence before docs edits.
- **DOCS (advisory):** documentation-heavy scope. README and release-maintainer docs are the primary deliverables; release note is in scope only for bounded public-facing context, not final publish checklist expansion.
- **IRIS (advisory):** readable planned files include README and release docs. Existing README `TODO`/`FIXME` matches are example strings inside code snippets; not actionable review debt. Watch for stale `personal fork` and `pi-ptc-next` active-framing copy.
- **DAVE (advisory):** `.github/workflows/ci.yml` exists and package scripts include `verify:ci`; plan should document CI parity accurately but should not change CI config unless tests reveal a narrow docs mismatch.
- **CODI (advisory):** skipped — markdown/docs/test-only scope has no stable TS/JS codegraph seeds.
- **ARCH/GABE/LUKE/ARIA/DANA/OMAR/PETE/REED/VERA (advisory):** skipped or no concerns — planned files do not add runtime architecture, API, UI, data schema, auth/privacy-sensitive flows, observability, resilience, or performance-sensitive behavior.
- **RUBY (advisory):** README is large (~970 lines) and historically accreted. Phase 59 may restructure README substantially, but should preserve accurate helper/API content and avoid broad source/runtime refactors.

[dispatch] pre-plan enforcement: DEAN PASS — `npm audit --json` reports `0 critical / 0 high / 3 moderate / 0 low`; command exits non-zero due moderate advisories only. No new critical/high blocker and no override required.

[dispatch] post-plan: TODD PASS — plan is `type: tdd` with RED-first Task 1 targeting `test/release-readiness.test.ts`; no post-plan restructuring required.
</module_dispatch>

<acceptance_criteria>

## AC-1: README gives new users a coherent public package path
```gherkin
Given a new user opens the README for `pi-ptc-advanced`
When they scan the first major sections
Then the README explains what the extension does, why `code_execution` exists, how to install from the current repo/GitHub path without using npm registry install claims, and how to run a minimal quickstart before deep internals
And the README includes sections for Quick Start, Installation, Usage, Python helpers, Configuration or Environment Variables, Verification, Limitations, Development, Troubleshooting, License, and Credits/lineage
And fork/upstream lineage remains present but does not dominate the opening path.
```

## AC-2: Public docs avoid stale personal-fork/current-identity framing
```gherkin
Given active public docs are inspected
When README, `docs/personal-fork-maintenance.md`, and `docs/releases/1.0.0.md` are searched
Then active install/update/release-baseline instructions point at `pi-ptc-advanced@1.0.0` and/or the intended `coctostan/pi-ptc-advanced` identity without presenting an npm registry install command before publish is confirmed
And stale `pi-ptc-next` references are limited to lineage, historical release notes, or explicit repo-rename caveats
And `personal fork` wording is not used as the main active public release framing.
```

## AC-3: Release and maintainer docs preserve manual boundaries
```gherkin
Given Phase 60 and Phase 61 still own final release gate and repo rename proof
When the Phase 59 docs are complete
Then `docs/releases/1.0.0.md` and the maintainer runbook clearly state that `npm publish`, dist-tags, git tags, GitHub releases, and actual GitHub repository rename are manual/user-owned
And the docs do not claim those actions have already happened
And `npm run verify:release-package`, `npm run verify:ci`, and focused release-readiness tests remain the documented verification path.
```

</acceptance_criteria>

<tasks>

<task type="tdd">
  <name>Task 1: Extend release-readiness docs drift tests for README/docs polish (RED first)</name>
  <files>test/release-readiness.test.ts</files>
  <action>
    Update `test/release-readiness.test.ts` before docs implementation so it asserts the Phase 59 documentation contract:
    - README has user-facing sections for Quick Start, Installation, Usage, Python helpers or helper surface, Configuration/Environment Variables, Verification, Limitations, Development, Troubleshooting, License, and Credits/lineage.
    - README opening path presents product value and quickstart/install guidance before detailed fork lineage and deep internals.
    - README/runbook/release note preserve manual publish/repo-rename boundaries and do not imply publish/tag/release/rename already happened.
    - README does not include `npm install pi-ptc-advanced` / `pi install pi-ptc-advanced` registry-style install commands until actual npm publish is confirmed; GitHub/source install framing is acceptable.
    - Active README/runbook/release note copy does not use `personal fork` as the primary public release framing. Do not forbid historical links or filename references where needed for compatibility.
    - Existing Phase 58 identity tests for `pi-ptc-advanced@1.0.0`, repo metadata, release note, changelog, and verifier behavior remain intact.
    Run `node --test test/release-readiness.test.ts` immediately after this edit and record RED evidence; expected failures should point at missing/stale docs structure, not syntax errors.
  </action>
  <verify>node --test test/release-readiness.test.ts (expect RED before docs edits, then GREEN after Tasks 2-3)</verify>
  <done>AC-1, AC-2, and AC-3 are test-specified for README/docs structure, stale framing, and manual-boundary claims.</done>
</task>

<task type="auto">
  <name>Task 2: Restructure README into a public package front door</name>
  <files>README.md</files>
  <action>
    Perform a larger top-to-bottom README rewrite/reorganization to satisfy the Phase 59 public docs contract while preserving accurate technical content:
    - Keep `# pi-ptc-advanced` and a concise product-value opening.
    - Add or elevate a copy-pasteable Quick Start before deep internals.
    - Make Installation and Usage explain the package identity, current GitHub/source install path, Pi extension setup, and a minimal `code_execution` example without adding npm registry install commands or claiming external publish has already happened.
    - Keep the helper surface accurate: direct callable Pi wrappers are async; `ptc.*` helpers include batching/reduction/report/handle/test helpers; `nu` remains preferred for pipeline-style filesystem/data analysis.
    - Add a compact Verification section using actual scripts from `package.json`: `npm run build`, `npm test`, `npm run verify:ci`, and `npm run verify:release-package` with the known local Node caveat only if still relevant.
    - Add/clarify Limitations and Manual release boundaries: no automatic `npm publish`, tags, GitHub releases, or repo rename.
    - Keep Credits and lineage present near the bottom, with upstream and `pi-ptc-next` history accurate but not leading.
    - Remove or relocate most deep implementation/history material from the opening path; a larger rewrite is preferred over a minimal section shuffle.
    Avoid changing runtime/source behavior, generated package files, or historical release notes.
  </action>
  <verify>node --test test/release-readiness.test.ts && npm run build</verify>
  <done>AC-1 and AC-2 satisfied for README structure, active identity, quickstart/install/usage clarity, and lineage placement.</done>
</task>

<task type="auto">
  <name>Task 3: Polish public release maintainer docs and 1.0 release note</name>
  <files>docs/personal-fork-maintenance.md, docs/releases/1.0.0.md</files>
  <action>
    Update the active release-maintainer docs enough to align with the polished README:
    - Keep `docs/personal-fork-maintenance.md` path for compatibility, but make the title/body consistently public release maintenance rather than personal-fork-first framing.
    - Link back to README, `CHANGELOG.md`, and `docs/releases/1.0.0.md` where useful.
    - Clarify local verification (`verify:personal`, `verify:personal:full`, `verify:ci`, `verify:release-package`) and when each is used.
    - Expand `docs/releases/1.0.0.md` with a bounded user-facing release summary, verification checklist, manual publish boundary, and repo-rename caveat without duplicating Phase 60's final release-gate work.
    - Do not claim npm publish, git tags, GitHub releases, or actual repository rename occurred.
  </action>
  <verify>node --test test/release-readiness.test.ts && npm run build && bash scripts/verify-release-package.sh</verify>
  <done>AC-2 and AC-3 satisfied for active release-maintainer docs, release note, verification path, and manual-boundary wording.</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- Do not run `npm publish`, create npm dist-tags, create git tags, or create GitHub releases.
- Do not perform the actual GitHub repository rename; Phase 61 owns rename/migration proof and any human-action checkpoint.
- Do not remove upstream credit, license attribution, or lineage history; move/reframe rather than delete.
- Do not refactor runtime behavior (`src/**`, `src/python-runtime/**`) as part of docs polish.
- Do not edit package metadata, package-lock metadata, dependency versions, or CI config unless a narrow docs/test mismatch makes it unavoidable and the deviation is recorded.

## SCOPE LIMITS
- This phase is README/docs polish, not final release-gate enrichment; Phase 60 owns final publish checklist depth, final release note/changelog polish, and any last release-readiness expansion.
- Historical release notes may remain historical; do not rewrite old release notes to erase prior names.
- Keep README examples accurate and bounded; avoid inventing Pi commands, APIs, badges, registry status, or unsupported provider claims.
- Local full `npm test` may fail under older local Node versions because some existing tests use `--experimental-transform-types`; do not treat that environment mismatch as a Phase 59 docs regression unless CI also fails.
- Do not add npm registry install commands (`npm install pi-ptc-advanced` or equivalent) until actual npm publish is confirmed by the user; use GitHub/source install or verification language instead.

</boundaries>

<verification>
Before declaring plan complete:
- [ ] `node --test test/release-readiness.test.ts` fails RED after Task 1 for docs-structure/stale-framing gaps and passes after Tasks 2-3.
- [ ] `npm run build` passes.
- [ ] `bash scripts/verify-release-package.sh` passes against `pi-ptc-advanced@1.0.0`.
- [ ] `npm audit --json` counts are recorded; critical/high remain 0 or are handled through DEAN workflow.
- [ ] README active public package path includes Quick Start, Installation, Usage, helper surface, verification, limitations, development, troubleshooting, license, and credits/lineage.
- [ ] Active README/runbook/release note preserve manual publish/repo-rename boundaries and avoid stale current-identity framing.
- [ ] All acceptance criteria met.
</verification>

<success_criteria>
- README reads like a public package README for `pi-ptc-advanced@1.0.0`, not a fork-history note or internal implementation diary.
- A new user can identify the value proposition, current non-registry install path, quickstart, usage model, verification commands, helper surface, limitations, and support/development path from README alone.
- Release-maintainer docs and 1.0 release note align with README while preserving manual publish and repo-rename boundaries.
- Release-readiness tests guard the polished docs contract.
</success_criteria>

<output>
After completion, create `.paul/phases/59-readme-and-docs-polish/59-01-SUMMARY.md`.
</output>
