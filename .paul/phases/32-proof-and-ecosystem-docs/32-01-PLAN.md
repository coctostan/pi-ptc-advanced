---
phase: 32-proof-and-ecosystem-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - src/index.ts
  - test/index.test.ts
  - test/orchestration-ecosystem-helper.test.ts
  - test/orchestration-ecosystem-contract.test.ts
autonomous: true
---

<objective>
## Goal
Close Milestone 12 by adding the missing proof and user-facing guidance for the Phase 30/31 orchestration helpers so `ptc.batch_tool(...)`, `ptc.first_success(...)`, `ptc.reduce_tool(...)`, and `ptc.fit_output(...)` are documented accurately and proven in focused ecosystem-style `code_execution` workflows.

## Purpose
Phase 30 and Phase 31 intentionally shipped helper/runtime behavior first. Phase 32 should finish the milestone the same way Phase 29 closed Milestone 11: keep runtime behavior unchanged, add focused execution-level proof through the real `CodeExecutor` path, surface the shipped helper APIs in the generated `code_execution` description, and document compact examples for composing hashline-style, codegraph-style, and web-handle workflows without reopening broader runtime design.

## Output
- Focused execution-level proof for ecosystem-style orchestration/output-budget workflows
- Updated `code_execution` tool description text for the Milestone 12 helper surface
- Updated `README.md` coverage for `ptc.batch_tool(...)`, `ptc.first_success(...)`, `ptc.reduce_tool(...)`, and `ptc.fit_output(...)`
- Compact README examples showing hashline-style, codegraph-style, and web-handle composition patterns
- Focused doc-contract coverage that keeps the README claims aligned with the shipped runtime helper surface
</objective>

<context>
## Project Context
.paul/PROJECT.md
.paul/ROADMAP.md
.paul/STATE.md

## Prior Work (only if genuinely needed)
.paul/phases/30-core-orchestration-primitives/30-01-SUMMARY.md
.paul/phases/31-bounded-reduction-and-output-budget-helpers/31-01-SUMMARY.md
.paul/phases/29-proof-and-docs/29-01-SUMMARY.md

## Source Files
README.md
src/index.ts
test/index.test.ts
test/orchestration-helper.test.ts
test/reduction-helper.test.ts
test/hashline-interop-smoke.test.ts
test/handle-helper-contract.test.ts
</context>

<module_dispatch>
## Pre-Plan Module Dispatch
[dispatch] pre-plan advisory: ARCH(75) → layered package layout (`src/contracts`, `src/execution`, `src/python-runtime`, `src/tools`) fits a docs/proof slice with no architectural pressure to reopen runtime plumbing | TODD(100) → focused `node:test` helper-proof precedent already exists in adjacent phases | IRIS(150) → no secret/eval/TODO-style blockers were found in the planned files beyond expected historical `any` usage inside existing tests | DAVE(200) → `.github/workflows/ci.yml` already covers the repo verification path | DOCS(200) → `README.md` is the right maintainer/model-facing surface for this milestone closure | RUBY(250) → `README.md` (~761 lines), `src/index.ts` (~418 lines), and `test/index.test.ts` (~1214 lines) are already large, so keep edits narrow and move new proof into dedicated focused test files

- ARCH: The planned work stays in `README.md`, `src/index.ts`, and focused `test/` files. No circular dependency risk or layer violation is expected for a proof/docs-only slice.
- TODD: Existing focused helper-proof files (`test/orchestration-helper.test.ts`, `test/reduction-helper.test.ts`, `test/handle-helper-contract.test.ts`) show the preferred pattern. Phase 32 should add a small dedicated ecosystem-proof file instead of broadening large omnibus suites.
- IRIS: Pre-plan scans found no hardcoded secrets, `eval(...)`, or TODO/FIXME/HACK/XXX blockers in the planned files. Keep examples concrete and avoid vague catch-all prose.
- DAVE: `.github/workflows/ci.yml` already validates the repo via build/test verification, so this phase should prove itself with the existing `npm run build`, focused `node --test ...`, and `npm test` path rather than inventing new CI behavior.
- DOCS: `README.md` already carries the primary helper guidance and combined-stack notes, so it is the correct place to document Milestone 12’s orchestration/output-budget usage patterns without starting a wider docs sweep.
- RUBY: Because the existing README and registration test file are already large, any new assertions or prose must stay disciplined; prefer dedicated new contract/proof tests for new claims.

[dispatch] pre-plan enforcement: DEAN(50) → no block | SETH(80) → no block
- DEAN audit baseline: critical `0` / high `2` / moderate `2` / low `1` (`npm audit --json`). No critical vulnerabilities block planning, but the non-zero baseline must remain stated honestly during verification.
- SETH: No hardcoded-secret hits were found in the planned files, and this docs/proof slice does not expand auth or credential-handling scope.
</module_dispatch>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)
No specialized flows configured (`.paul/SPECIAL-FLOWS.md` not found).
</skills>

<acceptance_criteria>
## AC-1: Ecosystem-style orchestration helpers are proven through the live execution path
```gherkin
Given a `code_execution` session with deterministic callable tools that resemble hashline-style, codegraph-style, and web-handle workflows
When Python composes `ptc.batch_tool(...)`, `ptc.first_success(...)`, `ptc.reduce_tool(...)`, `ptc.fit_output(...)`, and existing bounded helper surfaces inside a real `CodeExecutor` run
Then the resulting proof shows ordered orchestration, bounded fallback behavior, and compact output handling without changing runtime semantics
And the examples stay focused in dedicated proof files rather than widening existing omnibus tests
```

## AC-2: The `code_execution` tool description exposes the Milestone 12 helper surface accurately
```gherkin
Given Pi registers the `code_execution` tool for a session
When the tool description is generated
Then it includes `ptc.batch_tool(...)`, `ptc.first_success(...)`, `ptc.reduce_tool(...)`, and `ptc.fit_output(...)`
And it explains when these helpers are useful for keeping multi-tool orchestration and bounded output processing inside Python
And it avoids reopening operator-only config or runtime-internals guidance
```

## AC-3: README guidance documents ecosystem-oriented composition for Milestone 12
```gherkin
Given a maintainer or model reads `README.md`
When they review the Python helper guidance for Milestone 12
Then they can see the documented orchestration/output-budget helper surface
And they can follow compact examples for hashline-style search/inspect reduction, codegraph-style fallback selection, and web-handle follow-up with bounded output fitting
And the README claims remain protected by focused tests so docs and implementation stay aligned
```
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Task 1: Add focused execution-level proof for ecosystem-style orchestration helper workflows</name>
  <files>test/orchestration-ecosystem-helper.test.ts</files>
  <action>
    Create a small dedicated runtime-proof test file that exercises the already-shipped Phase 30/31 helper surface through `CodeExecutor` instead of growing the existing orchestration/reduction proof files.
    Coverage requirements:
    - stand up deterministic callable-tool stubs that resemble three ecosystem patterns this repo already references: hashline-style structured file/search results, codegraph-style fallback/selection results, and web-tool response/file handle follow-up
    - prove at least one workflow that uses `ptc.batch_tool(...)` or `ptc.reduce_tool(...)` to keep multi-step intermediate results local while returning a compact summary
    - prove `ptc.first_success(...)` in an ordered fallback scenario that mirrors codegraph/optional-tool selection behavior without reintroducing concurrent race semantics
    - prove `ptc.fit_output(...)` is used to bound the final returned payload for a handle-rich or nested result shape
    - keep the proof runtime-focused and isolated; do not change `src/python-runtime/runtime.py` unless a real defect is discovered while proving already-shipped behavior
    Avoid: broadening `test/code-executor.test.ts`, changing helper semantics, or depending on external network/packages for the proof.
  </action>
  <verify>npm run build && node --test test/orchestration-ecosystem-helper.test.ts</verify>
  <done>AC-1 is satisfied: a focused real-execution proof demonstrates Milestone 12 helper composition across representative ecosystem-style workflows.</done>
</task>

<task type="auto">
  <name>Task 2: Surface the Milestone 12 orchestration/output-budget helpers in the `code_execution` tool description</name>
  <files>src/index.ts, test/index.test.ts</files>
  <action>
    Update `buildToolDescription(...)` so the registered `code_execution` tool advertises the current orchestration helper surface accurately and concisely.
    Implementation requirements:
    - add `await ptc.batch_tool(calls, max_concurrency=None) -> list[Any]`
    - add `await ptc.first_success(calls, max_concurrency=None) -> Any`
    - add `await ptc.reduce_tool(calls, reducer, initial, max_concurrency=None) -> Any`
    - add `ptc.fit_output(value, max_chars=None, max_items=None, max_depth=None) -> dict[str, Any]`
    - add one short usage note that steers models toward these helpers for repeated multi-tool orchestration and bounded final output shaping
    - extend the existing tool-registration assertions so the generated description is locked to the new helper text
    - preserve the already-shipped optional-tool introspection guidance from Milestone 11
    Avoid: changing auto-routing behavior, runtime policy semantics, or stuffing the description with long README-style examples.
  </action>
  <verify>npm run build && node --test test/index.test.ts</verify>
  <done>AC-2 is satisfied: the registered `code_execution` description matches the shipped Milestone 12 helper surface and keeps the guidance concise.</done>
</task>

<task type="auto">
  <name>Task 3: Document Milestone 12 ecosystem composition patterns in README and lock them with focused doc-contract coverage</name>
  <files>README.md, test/orchestration-ecosystem-contract.test.ts</files>
  <action>
    Update `README.md` so Milestone 12's user-facing guidance is complete and concrete without turning this phase into a broad docs rewrite.
    Documentation requirements:
    - add `ptc.batch_tool(...)`, `ptc.first_success(...)`, `ptc.reduce_tool(...)`, and `ptc.fit_output(...)` to the helper list
    - explain when to use the orchestration helpers versus direct one-off tool calls
    - add compact Python examples that cover: (1) hashline-style search/inspect reduction, (2) codegraph-style ordered fallback/selection, and (3) web-handle follow-up with bounded `ptc.fit_output(...)` output
    - keep examples aligned with the already-documented `ptc.list_callable_tools()` / handle helper constraints instead of promising unsupported graph handles or new runtime APIs
    - add a focused contract test that keeps the README claims aligned with the shipped helper/runtime surface and the new tool-description guidance
    Avoid: a broad README rewrite, `docs/**` sweep, changelog updates, or new claims about unsupported helper categories.
  </action>
  <verify>npm run build && node --test test/orchestration-ecosystem-contract.test.ts test/orchestration-ecosystem-helper.test.ts test/index.test.ts && npm test</verify>
  <done>AC-3 is satisfied: `README.md` documents the Milestone 12 helper surface with bounded ecosystem examples, and focused tests keep those claims aligned with implementation.</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- `src/python-runtime/runtime.py`, `src/code-executor.ts`, and tool-registry/runtime policy plumbing unless a real defect is discovered while proving the already-shipped Milestone 12 behavior
- Release/version/changelog artifacts (`package.json` version, `CHANGELOG.md`, release notes)
- Existing hashline bridge mechanics or optional-tool exposure policy semantics
- Unrelated docs under `docs/**` unless an unavoidable broken cross-reference is found

## SCOPE LIMITS
- No new helper APIs beyond documenting and proving the already-shipped Milestone 12 helpers
- No broad test-suite refactor; prefer dedicated focused tests over expanding `test/code-executor.test.ts`
- No graph-handle ergonomics or broader runtime reflection work
- No broad README cleanup outside the helper/proof/examples surface required for this milestone closure
</boundaries>

<verification>
Before declaring plan complete:
- [ ] `npm run build`
- [ ] `node --test test/orchestration-ecosystem-helper.test.ts`
- [ ] `node --test test/orchestration-ecosystem-contract.test.ts`
- [ ] `node --test test/index.test.ts`
- [ ] `npm test`
- [ ] Dependency audit baseline remains honest at `0 critical / 2 high / 2 moderate / 1 low`
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- Execution-level proof covers representative ecosystem-style usage of `ptc.batch_tool(...)`, `ptc.first_success(...)`, `ptc.reduce_tool(...)`, and `ptc.fit_output(...)` against a real `CodeExecutor` session
- The registered `code_execution` tool description exposes the full Milestone 12 helper surface without reopening runtime/policy scope
- `README.md` documents the shipped helper surface plus compact hashline/codegraph/web-style examples
- Focused tests keep the proof/docs surface aligned without reopening Phase 30/31 implementation scope
- Existing helper/runtime behavior and policy semantics remain unchanged
</success_criteria>

<output>
After completion, create `.paul/phases/32-proof-and-ecosystem-docs/32-01-SUMMARY.md`
</output>
