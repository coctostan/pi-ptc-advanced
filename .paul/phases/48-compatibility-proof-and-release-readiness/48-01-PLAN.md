---
phase: 48-compatibility-proof-and-release-readiness
plan: 01
type: tdd
wave: 1
depends_on: ["45-01", "46-01", "47-01"]
files_modified:
  - package.json
  - package-lock.json
  - scripts/verify-release-package.sh
  - test/release-readiness.test.ts
  - README.md
  - CHANGELOG.md
  - docs/personal-fork-maintenance.md
  - docs/releases/0.16.0.md
autonomous: false
---

<objective>
## Goal
Finalize the bounded `0.16.0` compatibility proof and release-readiness surface for `pi-ptc-advanced`, with explicit evidence for Pi compatibility, package versioning, release verification, and the acknowledged dependency-audit risk baseline.

## Purpose
Milestone 17 has already audited Pi compatibility, aligned the runtime to latest Mario-scope Pi packages, and added Pi prompt metadata integration. Phase 48 closes the milestone by turning that work into a coherent release-candidate surface: version metadata, release notes, maintainer docs, verification proof, and a human-visible gate around the current critical dependency advisories.

## Output
- `pi-ptc-advanced` package metadata and release-package verification target `0.16.0`.
- A focused release-readiness regression test prevents version/doc/script drift.
- README, changelog, maintainer runbook, and `docs/releases/0.16.0.md` describe the compatibility milestone accurately.
- Current dependency-audit findings are acknowledged and documented as a release-readiness decision point, not silently treated as clean.
- Full local verification and CI-parity commands are run before UNIFY.
</objective>

<context>
## Project Context
- `.paul/STATE.md`: Phase 48 is ready to plan; carry-forward risk is `npm audit --json` at 4 critical / 0 high / 3 moderate after the Phase 47 DEAN check.
- `.paul/ROADMAP.md`: Phase 48 focus is compatibility proof and release readiness for the `0.16.0` milestone.
- `.paul/PROJECT.md`: active requirement is Phase 48 compatibility proof and release readiness: final Pi compatibility evidence, dependency-audit advisories, CI/release proof, and bounded release notes.
- `pals.json`: GitHub Flow is enabled; APPLY must work on a non-base feature branch and UNIFY owns the PR/CI/merge gate.

## Prior Work
- `.paul/phases/45-pi-api-and-documentation-delta-audit/45-01-PI-COMPAT-AUDIT.md`: established the Pi compatibility delta, prompt-integration findings, and Phase 46/47/48 remediation handoff.
- `.paul/phases/46-extension-runtime-compatibility-alignment/46-01-SUMMARY.md`: aligned runtime/package compatibility to latest Mario-scope Pi packages (`@mariozechner/*@0.73.1`) while preserving the decision not to claim a hard `@earendil-works/*` migration.
- `.paul/phases/47-system-prompt-and-tool-guidance-optimization/47-01-SUMMARY.md`: shipped `code_execution` prompt metadata, idempotent `systemPromptOptions`-aware auto-routing, custom-tool prompt metadata preservation, and the dependency-audit carry-forward.

## Source Files
- `package.json` / `package-lock.json` — release package version and lockfile root metadata.
- `scripts/verify-release-package.sh` — release-package assertion currently hardcodes the `0.15.0` baseline.
- `test/release-readiness.test.ts` — new focused regression test for release metadata/docs/script alignment.
- `README.md` — install/maintenance release baseline and release-doc links currently reference `0.15.0`.
- `CHANGELOG.md` — needs a `0.16.0` release section that promotes the Phase 46/47 compatibility work and records the audit caveat.
- `docs/personal-fork-maintenance.md` — maintainer runbook release target and verification notes currently reference `0.15.0`.
- `docs/releases/0.16.0.md` — new release note for the compatibility/prompt-integration milestone.
</context>

<module_dispatch>
## Pre-Plan Module Dispatch

[dispatch] pre-plan advisory: modules.yaml loaded (kernel 2.0.0); advisory modules evaluated for planned files `package.json`, `package-lock.json`, `scripts/verify-release-package.sh`, `test/release-readiness.test.ts`, `README.md`, `CHANGELOG.md`, `docs/personal-fork-maintenance.md`, `docs/releases/0.16.0.md`.

### TODD
| Framework | Test Files | Status |
|---|---:|---|
| node:test | 39 existing `test/*.test.ts`; new `test/release-readiness.test.ts` planned | PASS — test infrastructure detected |

`tdd_candidates`: existing doc-contract tests cover README/tool-description/helper guidance, but no dedicated release metadata drift test exists. This plan is type `tdd` and creates the release-readiness test before changing release metadata/docs.

### ARCH
Detected pattern: small TypeScript npm package / Pi extension with flat `src/`, `test/`, `scripts/`, `docs/`, and root docs structure (high confidence).

| Directory | Planned files | Status |
|---|---:|---|
| repo root metadata/docs | 4 | PASS |
| `scripts/` | 1 | PASS |
| `test/` | 1 | PASS |
| `docs/` | 2 | PASS |

Line-count warnings: `package-lock.json` is generated and large; `README.md` is already ~850 lines. Keep README changes bounded and prefer the new focused release note for detail.

### SETH
| Category | Status |
|---|---|
| Secrets | PASS — no hardcoded secret patterns in planned existing files |
| Auth / API | PASS — no auth/API surface in scope |
| Injection risk | PASS — release metadata/docs and shell verification only |

### IRIS
| File | Pattern | Count |
|---|---|---:|
| `README.md` | literal example strings `TODO` / `FIXME` | 2 |
| Planned package/script/docs files | TODO/FIXME/HACK/XXX | 0 |

README matches are examples already known from Phase 47, not implementation debt.

### DAVE
| File | Platform | Status |
|---|---|---|
| `.github/workflows/ci.yml` | GitHub Actions | PASS — uses Node 22, installs required external tooling, and runs `npm run verify:ci` |

Coverage notes: `verify:ci` chains focused verification, full verification, and release-package verification. Phase 48 will update the release-package target and rerun the chain.

### DOCS
| Source File | Related Doc | Status |
|---|---|---|
| `package.json`, `package-lock.json` | `README.md`, `CHANGELOG.md`, `docs/releases/0.16.0.md` | PLANNED |
| `scripts/verify-release-package.sh` | `README.md`, `docs/personal-fork-maintenance.md`, `docs/releases/0.16.0.md` | PLANNED |
| `test/release-readiness.test.ts` | release-readiness proof artifact | n/a |

### DANA / GABE / ARIA / LUKE / VERA
No data/schema/migration files, API route files, UI component files, or privacy-sensitive data model files are planned. These modules skip cleanly.

### OMAR / PETE / REED / RUBY
| Module | Finding |
|---|---|
| OMAR | `scripts/verify-release-package.sh` uses console output inside inline Node verification blocks; this is expected for CLI verification, not production logging. |
| PETE | No performance-sensitive runtime path or heavy imports planned. |
| REED | Shell verification uses `set -euo pipefail` and temporary install cleanup trap; preserve those safeguards. |
| RUBY | Debt awareness: avoid broad README growth and do not hand-edit unrelated generated package-lock content beyond the root version metadata produced by npm tooling. |

### Blast Radius (CODI)
[dispatch] CODI: skipped — no extractable symbols in phase scope

This phase changes package metadata, shell verification, tests, and docs. No existing TS/JS source selectors with stable top-level runtime symbols are in scope.

## Pre-Plan Enforcement Dispatch

[dispatch] pre-plan enforcement: DEAN OVERRIDE ACKNOWLEDGED / PASS — `npm audit --json` currently reports 4 critical / 0 high / 3 moderate / 0 low. User selected override during `/paul:plan`; `.paul/dean-baseline.json` was updated with the current severity counts and package list, valid through 2026-06-11. Phase 48 must document and gate this acknowledged dependency-risk baseline instead of claiming a clean audit.

## Post-Plan Dispatch

[dispatch] post-plan: TODD PASS — plan type is `tdd`; tasks are ordered RED → GREEN → DOCS/verification → human release-readiness verification checkpoint, so no post-plan restructure was required.
</module_dispatch>

<acceptance_criteria>
## AC-1: Release metadata and verification target `0.16.0`
```gherkin
Given the fork is preparing the Milestone 17 compatibility release candidate
When package metadata and release verification are inspected
Then `package.json`, the lockfile root metadata, and `scripts/verify-release-package.sh` consistently target `pi-ptc-advanced@0.16.0`
```

## AC-2: Release-readiness drift is covered by focused tests
```gherkin
Given release metadata, docs, and verification scripts must stay aligned
When `node --test test/release-readiness.test.ts` runs
Then it fails on stale `0.15.0` release targets, missing `0.16.0` release docs, missing audit caveat text, or mismatched verification-script assertions
```

## AC-3: User-facing release docs accurately describe compatibility proof and risk
```gherkin
Given a maintainer reads README, CHANGELOG, personal-fork maintenance docs, or the `0.16.0` release note
When they look for the current release baseline
Then they see the Phase 45/46/47 compatibility and prompt-integration outcomes, the latest Mario-scope compatibility target, the manual publish boundary, and the acknowledged dependency-audit findings without any claim of a clean audit or hard `@earendil-works/*` migration
```

## AC-4: Release-readiness verification is green and human-gated
```gherkin
Given the release candidate has updated metadata, docs, tests, and audit caveat
When APPLY finishes
Then `npm test`, `npm run build`, `npm run verify:release-package`, and `npm run verify:ci` pass locally, and the user explicitly approves whether to treat `0.16.0` as release-ready with the acknowledged audit baseline
```
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Task 1 RED: Add release-readiness drift coverage</name>
  <files>test/release-readiness.test.ts</files>
  <action>
    Add a focused failing `node:test` suite before changing release metadata:
    - Read `package.json`, `package-lock.json`, `scripts/verify-release-package.sh`, `README.md`, `CHANGELOG.md`, `docs/personal-fork-maintenance.md`, and `docs/releases/0.16.0.md` when present.
    - Assert the intended release target is `pi-ptc-advanced@0.16.0` across package metadata and release verification script assertions.
    - Assert README/runbook links point to `docs/releases/0.16.0.md` and no longer describe `0.15.0` as the active baseline.
    - Assert the changelog/release note mention the Pi compatibility/prompt metadata outcomes and the acknowledged dependency-audit baseline.
    - Keep the test deterministic and file-local; avoid network calls, shelling out to npm, or broad snapshot assertions.
  </action>
  <verify>npm run build && node --test test/release-readiness.test.ts</verify>
  <done>RED coverage exists and initially fails on the stale `0.15.0` release baseline and missing `0.16.0` release note; AC-1, AC-2, and AC-3 are represented by tests.</done>
</task>

<task type="auto">
  <name>Task 2 GREEN: Align package metadata and release verification to 0.16.0</name>
  <files>package.json, package-lock.json, scripts/verify-release-package.sh</files>
  <action>
    Update the package/release verification surface:
    - Bump package metadata to `0.16.0` using npm-compatible metadata updates so `package-lock.json` root package version stays aligned.
    - Update `scripts/verify-release-package.sh` expected version checks from `0.15.0` to `0.16.0` while preserving existing package-name checks, tarball required/forbidden entries, temporary install proof, and cleanup trap.
    - Do not change dependency versions, package scopes, repository metadata, tarball file allowlists beyond the version target, or CI workflow behavior.
  </action>
  <verify>npm run build && node --test test/release-readiness.test.ts && npm run verify:release-package</verify>
  <done>AC-1 and AC-2 pass for package metadata, lockfile metadata, release verification assertions, and installable tarball proof.</done>
</task>

<task type="auto">
  <name>Task 3: Document the 0.16.0 compatibility release candidate and run full verification</name>
  <files>README.md, CHANGELOG.md, docs/personal-fork-maintenance.md, docs/releases/0.16.0.md</files>
  <action>
    Update user- and maintainer-facing release docs after Task 2 passes:
    - Create `docs/releases/0.16.0.md` with concise release notes covering Phase 45 audit proof, Phase 46 latest Mario-scope compatibility alignment, Phase 47 prompt metadata integration, verification commands, manual publish boundary, and current dependency-audit caveat.
    - Promote the changelog `Unreleased` prompt-guidance entries into a `0.16.0` section and include release-readiness/compatibility proof notes.
    - Update README and `docs/personal-fork-maintenance.md` active baseline references from `0.15.0` to `0.16.0`, including release-doc links and package-check wording.
    - State plainly that the release candidate does not claim a hard `@earendil-works/*` migration and does not claim a clean dependency audit while the DEAN baseline is acknowledged.
    - Keep README edits bounded; put detailed narrative in `docs/releases/0.16.0.md`.
  </action>
  <verify>npm test && npm run build && npm run verify:release-package && npm run verify:ci</verify>
  <done>AC-2 and AC-3 pass in focused tests and full local verification; AC-4 programmatic verification is complete.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>`0.16.0` release-candidate metadata, verification script, release-readiness test, README/changelog/runbook updates, and release note with acknowledged dependency-audit caveat.</what-built>
  <how-to-verify>
    1. Review `docs/releases/0.16.0.md`, `CHANGELOG.md`, README release-baseline snippets, and `docs/personal-fork-maintenance.md`.
    2. Confirm the docs accurately state the latest Mario-scope compatibility target and do not claim a hard `@earendil-works/*` migration.
    3. Confirm the dependency-audit caveat is acceptable for this manual release candidate, or request remediation before UNIFY.
    4. Confirm local verification evidence: `npm test`, `npm run build`, `npm run verify:release-package`, and `npm run verify:ci` passed.
  </how-to-verify>
  <resume-signal>Type `approved` to treat `0.16.0` as release-ready with the acknowledged audit baseline, or describe required changes/remediation.</resume-signal>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- Runtime behavior in `src/**`.
- Python runtime helper semantics under `src/python-runtime/**`.
- Dependency scopes or versions other than the root package version metadata (`0.16.0`).
- GitHub Actions workflow behavior unless verification proves it is broken.
- Installed Pi packages, `node_modules/`, or home-directory Pi installs.

## SCOPE LIMITS
- This plan is release-readiness proof and documentation/metadata alignment only.
- Do not remediate dependency advisories unless the human verification checkpoint redirects to a fix loop; the current user decision was to acknowledge the DEAN baseline for planning.
- Do not implement publish automation, tag automation, npm registry publishing, or release GitHub Actions.
- Do not broaden README narrative; detailed release context belongs in `docs/releases/0.16.0.md`.
- Do not claim a clean audit, final npm publish, or hard `@earendil-works/*` runtime migration.
</boundaries>

<verification>
Before declaring plan complete:
- [ ] `npm run build && node --test test/release-readiness.test.ts` fails during RED for stale/missing release-readiness state, then passes after implementation.
- [ ] `npm test`
- [ ] `npm run build`
- [ ] `npm run verify:release-package`
- [ ] `npm run verify:ci`
- [ ] `npm audit --json` result is recorded in APPLY/UNIFY notes and docs accurately describe any remaining findings.
- [ ] Human verification checkpoint approves release-readiness wording and audit caveat, or redirects to remediation.
- [ ] All acceptance criteria met.
</verification>

<success_criteria>
- Package metadata, lockfile root metadata, and release verification script consistently target `pi-ptc-advanced@0.16.0`.
- Focused release-readiness tests prevent stale release baseline, doc-link, and audit-caveat drift.
- README, changelog, maintainer runbook, and `docs/releases/0.16.0.md` accurately present the compatibility milestone and known dependency-audit risk.
- Local full verification and CI-parity verification pass before UNIFY.
- The human checkpoint explicitly accepts or redirects the release-candidate decision with the acknowledged DEAN baseline.
</success_criteria>

<output>
After completion, create `.paul/phases/48-compatibility-proof-and-release-readiness/48-01-SUMMARY.md` with:
- Plan-vs-actual task results and deviations.
- Verification commands and pass/fail outcomes.
- Dependency-audit baseline and release-readiness checkpoint decision.
- Module execution reports including post-apply and post-unify evidence.
- Final Milestone 17 completion/readiness notes.
</output>
