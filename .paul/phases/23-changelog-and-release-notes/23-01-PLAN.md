---
phase: 23-changelog-and-release-notes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CHANGELOG.md
  - README.md
  - docs/personal-fork-maintenance.md
  - docs/releases/0.8.0.md
autonomous: true
---

<objective>
## Goal
Create the release-documentation surface for `0.8.0` by adding a real `CHANGELOG.md`, writing a dedicated release-notes artifact, and linking the new release docs from the normal maintainer-facing surfaces.

## Purpose
Phase 22 established a coherent `0.8.0` package baseline, but the repo still lacks the actual release narrative maintainers and future readers need. Phase 23 should convert the shipped work into durable release docs so the fork has a clear changelog, a concrete 0.8.0 release-notes snapshot, and straightforward pointers from the existing README and maintenance runbook.

## Output
- A top-level `CHANGELOG.md` with an initial `0.8.0` release entry
- A dedicated `0.8.0` release-notes document for maintainers/users of the fork
- README/runbook links that point at the new release docs without claiming CI or publish automation exists
</objective>

<context>
## Project Context
.paul/PROJECT.md
.paul/ROADMAP.md
.paul/STATE.md

## Prior Work
.paul/phases/22-release-version-and-packaging/22-01-SUMMARY.md
.paul/phases/20-fork-workflow-and-local-maintenance/20-01-SUMMARY.md

## Source Files
README.md
docs/personal-fork-maintenance.md

## Planning Notes
- `CHANGELOG.md` is currently missing.
- Phase 22 already aligned the package/release baseline to `0.8.0`, added `LICENSE`, and introduced `npm run verify:release-package`.
- CI is still absent (`.github/workflows/**` missing) and remains Phase 24 scope.
- Dependency audit baseline remains `0 critical / 2 high / 1 moderate / 1 low`; documenting that baseline is in scope, fixing it is not.
- No `.paul/SPECIAL-FLOWS.md` is configured for this work.
</context>

<acceptance_criteria>
## AC-1: The repo has a usable changelog for the current release baseline
```gherkin
Given a maintainer or reader wants to understand what shipped in 0.8.0
When they open the repository changelog
Then they can see a clear 0.8.0 entry that summarizes the fork’s shipped capabilities, packaging baseline, and intentionally deferred follow-up work
```

## AC-2: The 0.8.0 release notes are explicit and maintainer-usable
```gherkin
Given the personal fork is now operating against the 0.8.0 baseline
When a maintainer reads the release notes
Then they can understand what changed, how to install/verify the release, and what limitations or deferred items still remain
```

## AC-3: Existing maintainer docs point to the new release docs without overstating automation
```gherkin
Given the README and personal maintenance runbook are the normal repo entrypoints
When a maintainer follows those docs
Then they can discover the changelog and 0.8.0 release notes, while the docs still make clear that CI, publish automation, and git workflow automation are separate concerns
```
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Task 1: Create the initial changelog with a 0.8.0 release entry</name>
  <files>CHANGELOG.md</files>
  <action>
    Add a top-level changelog file for the fork.
    Use a simple durable structure with at least:
    - a brief preamble explaining that this changelog tracks the personal fork
    - a `0.8.0` entry dated for this release-prep milestone
    - grouped notes that capture the shipped runtime/tooling behavior, personal-fork maintenance workflow, and release-package baseline
    - an explicit note about what is still deferred (CHANGELOG/release notes land here now; CI/release automation remains Phase 24 work)
    Keep the entry grounded in what actually shipped across the completed milestones; avoid speculative future claims or upstream-PR framing.
  </action>
  <verify>grep -q '0.8.0' CHANGELOG.md && grep -q 'verify:release-package' CHANGELOG.md && grep -q 'CI' CHANGELOG.md</verify>
  <done>AC-1 satisfied: the repository now has a durable changelog entry for 0.8.0.</done>
</task>

<task type="auto">
  <name>Task 2: Write dedicated 0.8.0 release notes for the fork</name>
  <files>docs/releases/0.8.0.md, CHANGELOG.md</files>
  <action>
    Create a dedicated release-notes document for `0.8.0` under `docs/releases/`.
    Cover, at minimum:
    - what this release is for
    - install/update target (`coctostan/pi-ptc-next`)
    - verification commands (`verify:personal`, `verify:personal:full`, `verify:release-package`)
    - major shipped capabilities and maintainer workflow changes
    - known limitations / deferred items (no CI yet, no publish automation, dependency audit baseline unchanged, bridge teardown still future work)
    Keep the release notes concise but materially more specific than a changelog bullet list.
    Align the changelog wording with the same reality, but keep the detailed operator-facing guidance in the release-notes document.
  </action>
  <verify>grep -q 'coctostan/pi-ptc-next' docs/releases/0.8.0.md && grep -q 'verify:release-package' docs/releases/0.8.0.md && grep -q 'dependency audit' docs/releases/0.8.0.md</verify>
  <done>AC-2 satisfied: 0.8.0 has a dedicated release-notes artifact that explains install, verification, and remaining limits.</done>
</task>

<task type="auto">
  <name>Task 3: Link the release docs from the README and maintenance runbook</name>
  <files>README.md, docs/personal-fork-maintenance.md, CHANGELOG.md, docs/releases/0.8.0.md</files>
  <action>
    Update the normal maintainer-facing docs so they point directly at the new changelog and release notes.
    Keep the additions concise:
    - README should surface where to find the changelog and current release notes
    - the maintenance runbook should mention the release docs as reference material alongside the verification commands
    - neither doc should imply CI, tagging, or publish automation already exists
    Avoid turning the README into a full release-notes dump; link out instead.
  </action>
  <verify>grep -q 'CHANGELOG.md' README.md docs/personal-fork-maintenance.md && grep -q 'docs/releases/0.8.0.md' README.md docs/personal-fork-maintenance.md</verify>
  <done>AC-3 satisfied: maintainers can find the changelog and release notes from the repo’s standard entrypoints.</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- `src/**`, `test/**`, package behavior, or release-package verification logic from Phase 22
- `package.json` versioning or package metadata unless a doc-verification step proves a factual mismatch that must be corrected
- `.github/workflows/**`, release automation, publish automation, or git remote/branch operations

## SCOPE LIMITS
- Focus on release documentation only: changelog, release notes, and doc linkage
- Do not remediate dependency vulnerabilities in this phase; only document the current baseline if it matters to release notes
- Do not reopen archived upstream PR-prep material as an active execution path
</boundaries>

<verification>
Before declaring plan complete:
- [ ] `grep -q '0.8.0' CHANGELOG.md docs/releases/0.8.0.md README.md docs/personal-fork-maintenance.md`
- [ ] `grep -q 'verify:release-package' CHANGELOG.md docs/releases/0.8.0.md README.md docs/personal-fork-maintenance.md`
- [ ] `grep -q 'coctostan/pi-ptc-next' docs/releases/0.8.0.md`
- [ ] `npm run verify:personal`
- [ ] `npm run verify:release-package`
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- The repo has a durable `CHANGELOG.md` instead of a documented placeholder for future work
- `0.8.0` release notes exist as a stable reference artifact for this fork
- README and maintenance docs point at the new release docs without inflating the automation story
- Phase 24 can focus on CI/release verification automation rather than first inventing the release narrative
</success_criteria>

<output>
After completion, create `.paul/phases/23-changelog-and-release-notes/23-01-SUMMARY.md`
</output>
