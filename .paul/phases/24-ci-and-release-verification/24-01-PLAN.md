---
phase: 24-ci-and-release-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - package.json
  - scripts/verify-ci.sh
  - README.md
  - docs/personal-fork-maintenance.md
  - docs/releases/0.8.0.md
  - CHANGELOG.md
autonomous: true
---

<objective>
## Goal
Add a repo-owned CI verification path for the `0.8.0` fork baseline so GitHub can run the same release-readiness checks maintainers already use locally.

## Purpose
Phase 22 established the package surface and Phase 23 established the release narrative. This plan closes the remaining release-readiness gap by making CI and release verification repeatable without changing the core runtime behavior or pretending publish/git automation already exists.

## Output
- A new GitHub Actions workflow at `.github/workflows/ci.yml`
- A repo-local `verify:ci` entrypoint backed by `scripts/verify-ci.sh`
- Maintainer/release docs updated to describe the CI verification path and its boundaries
</objective>

<context>
## Project Context
.paul/PROJECT.md
.paul/ROADMAP.md
.paul/STATE.md

## Prior Work (only if genuinely needed)
.paul/phases/22-release-version-and-packaging/22-01-SUMMARY.md
.paul/phases/23-changelog-and-release-notes/23-01-SUMMARY.md

## Source Files
package.json
scripts/verify-personal-fork.sh
scripts/verify-release-package.sh
README.md
docs/personal-fork-maintenance.md
docs/releases/0.8.0.md
CHANGELOG.md
</context>

<module_dispatch>
## Pre-Plan Module Dispatch
[dispatch] pre-plan advisory: TODD(100) → no direct TDD candidates detected for the planned workflow/script/docs scope; IRIS(150) → no immediate anti-pattern flags surfaced from the planned release-hardening files; DAVE(200) → no CI config currently exists, Node.js project detected from `package.json`, recommended target is `.github/workflows/ci.yml` with repo-local verification commands; DOCS(200) → release-facing changes require README/runbook/release-notes/changelog alignment; RUBY(250) → keep documentation edits narrow because `README.md` is already large, but no debt block applies to the planned workflow/shell additions.

[dispatch] pre-plan enforcement: DEAN(50) → `npm audit --json` baseline is unchanged at 0 critical / 2 high / 1 moderate / 1 low, so no planning block; SETH(80) → no hardcoded-secret hits surfaced in the current shell/docs scope, so no planning block.
</module_dispatch>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)
No specialized flows configured.
</skills>

<acceptance_criteria>
## AC-1: GitHub Actions verifies the release baseline
```gherkin
Given the repository is pushed to GitHub
When the CI workflow runs
Then it installs dependencies and executes the repo-owned verification path for build, tests, and release-package validation without adding publish/tag automation
```

## AC-2: Local and CI verification use the same contract
```gherkin
Given a maintainer is preparing or validating the `0.8.0` fork baseline
When they inspect `package.json`, the CI workflow, and the maintainer docs
Then they can identify one repo-local CI command that mirrors the automated verification path and still see the manual git/tag/publish boundary called out explicitly
```

## AC-3: Release-facing docs stay truthful about automation scope
```gherkin
Given the changelog and release docs are reviewed after the CI work lands
When a maintainer reads the updated release guidance
Then the docs describe the new CI verification coverage while continuing to state that publish, tagging, and git workflow automation remain out of scope
```
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Task 1: Add a repo-local CI verification entrypoint</name>
  <files>package.json, scripts/verify-ci.sh</files>
  <action>
    Add a dedicated `verify:ci` script in `package.json` backed by a new fail-fast shell entrypoint at `scripts/verify-ci.sh`.
    The new script should run the existing verification commands in a deterministic order: focused maintenance verification, full-suite verification, and release-package verification.
    Keep the existing `verify:personal`, `verify:personal:full`, and `verify:release-package` contracts intact rather than folding their logic together.
    Avoid adding git operations, publish logic, version bumps, or dependency-remediation behavior to this command; it is a verification surface only.
  </action>
  <verify>npm run verify:ci</verify>
  <done>AC-2 satisfied: a single repo-local CI parity command exists and succeeds using the current verification surfaces.</done>
</task>

<task type="auto">
  <name>Task 2: Add GitHub Actions CI for release verification</name>
  <files>.github/workflows/ci.yml</files>
  <action>
    Create a GitHub Actions workflow that runs on the normal repository verification events (push and pull_request are sufficient unless the implementation discovers a narrower branch filter is materially safer).
    Use a stable Node setup consistent with the current repo baseline, install dependencies with `npm ci`, and run the repo-local CI verification entrypoint instead of duplicating command lists inline wherever practical.
    Keep the workflow verification-only: no publish job, no tag creation, no release upload, no secret-dependent deployment steps.
    Include the minimum expected workflow structure for maintainability (`actions/checkout`, `actions/setup-node`, npm cache, and explicit verification step naming).
  </action>
  <verify>grep -q 'actions/checkout' .github/workflows/ci.yml && grep -q 'actions/setup-node' .github/workflows/ci.yml && grep -q 'npm run verify:ci' .github/workflows/ci.yml</verify>
  <done>AC-1 satisfied: GitHub Actions is configured to run the repo-owned release-readiness verification path.</done>
</task>

<task type="auto">
  <name>Task 3: Align maintainer and release docs with the new CI path</name>
  <files>README.md, docs/personal-fork-maintenance.md, docs/releases/0.8.0.md, CHANGELOG.md</files>
  <action>
    Update the main README, the personal maintenance runbook, the `0.8.0` release notes, and the changelog so they describe the new CI verification path in the same terms.
    Make the docs explicit that CI now automates verification only, while git sync, tagging, publishing, and PR operations remain manual/user-directed.
    Keep the `0.8.0` release baseline stable; do not turn this phase into a new version bump or a publish-automation effort.
    Ensure the docs point maintainers at the new repo-local `verify:ci` command and the `.github/workflows/ci.yml` workflow without overstating guarantees.
  </action>
  <verify>grep -q 'verify:ci' package.json README.md docs/personal-fork-maintenance.md docs/releases/0.8.0.md CHANGELOG.md && grep -q '.github/workflows/ci.yml' README.md docs/personal-fork-maintenance.md docs/releases/0.8.0.md CHANGELOG.md</verify>
  <done>AC-2 and AC-3 satisfied: release-facing docs and changelog reflect the CI verification path and preserve the manual automation boundary.</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- `src/**` runtime behavior, Python runtime internals, and tool-registry logic are not part of this phase.
- Existing verification commands (`verify:personal`, `verify:personal:full`, `verify:release-package`) must remain valid and recognizable.
- Package/version identity for `0.8.0` must remain unchanged.

## SCOPE LIMITS
- Do not add npm publish automation, git tagging, release drafting, PR automation, or remote-management scripts.
- Do not remediate the current dependency-audit findings in this phase unless a verification step is blocked by them.
- Do not introduce broader doc rewrites outside the CI/release-verification story.
</boundaries>

<verification>
Before declaring plan complete:
- [ ] `npm run verify:ci`
- [ ] `grep -q 'actions/checkout' .github/workflows/ci.yml && grep -q 'actions/setup-node' .github/workflows/ci.yml && grep -q 'npm run verify:ci' .github/workflows/ci.yml`
- [ ] `grep -q 'verify:ci' package.json README.md docs/personal-fork-maintenance.md docs/releases/0.8.0.md CHANGELOG.md`
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The repository gains a verification-only CI workflow aligned with the repo-local release-readiness commands
- Maintainer/release docs describe the new CI path without implying publish/tag/git automation exists
</success_criteria>

<output>
After completion, create `.paul/phases/24-ci-and-release-verification/24-01-SUMMARY.md`
</output>
